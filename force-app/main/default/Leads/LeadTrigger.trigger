trigger LeadTrigger on Lead (before insert,before update) {
    /* List<Lead> leadList =Trigger.new;

    if (Trigger.isBefore &&  Trigger.isInsert) {
        System.debug('Lead Before Insert Trigger Called');

        for (lead eachlead : leadList) {
            System.debug('Lead Before Insert Trigger record name=>'+ eachlead.firstname+' '+eachlead.lastname );
            
        }
        
    }
    if (Trigger.isBefore && Trigger.isUpdate) {
        System.debug('Lead Before Update Trigger Called');

        for (lead eachlead : leadList) {
            System.debug('Lead Before Update Trigger record id=>' + eachlead.Id +' Lead Before Update Trigger record name=>' 
            +eachlead.firstname+' '+eachlead.lastname+ ' Lead Before Update Trigger record description=>'+ eachlead.Description);
            
        }
        
    } */

    



}